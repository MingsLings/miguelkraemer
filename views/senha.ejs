<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Acesso Amaldiçoado</title>

    <style>
        @keyframes shake {
            0% { transform: translate(0, 0); }
            20% { transform: translate(-10px, 5px); }
            40% { transform: translate(10px, -5px); }
            60% { transform: translate(-8px, 5px); }
            80% { transform: translate(8px, -5px); }
            100% { transform: translate(0, 0); }
        }

        body {
            margin: 0;
            background: #000;
            overflow: hidden;
            font-family: Arial, sans-serif;
        }

        .eyes-bg {
            position: fixed;
            inset: 0;
            background-image: url("/images/oio.jpg");
            background-size: 300px;
            background-repeat: repeat;
            background-position: center;
            opacity: 0.15;
            animation: moveEyes 25s linear infinite;
        }

        @keyframes moveEyes {
            from { background-position: 0 0; }
            to { background-position: -1000px 1000px; }
        }

        .container {
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 2;
            position: relative;
        }

        .box {
            background: #0b0b0b;
            padding: 40px;
            border-radius: 12px;
            border: 2px solid #c62828;
            box-shadow: 0 0 25px red;
            text-align: center;
            width: 380px;
        }

        .kanji {
            font-size: 60px;
            color: red;
        }

        h1 {
            color: #ff2a2a;
            margin-bottom: 8px;
            letter-spacing: 3px;
        }

        p {
            font-size: 13px;
            color: #aaa;
            margin-bottom: 20px;
        }

        input {
            width: 100%;
            padding: 12px;
            border-radius: 8px;
            border: none;
            background: #111;
            color: red;
            font-size: 16px;
            outline: none;
            border: 1px solid #333;
        }

        button {
            width: 100%;
            padding: 12px;
            margin-top: 18px;
            border-radius: 8px;
            border: none;
            background: #c62828;
            color: white;
            font-size: 15px;
            cursor: pointer;
            transition: 0.2s;
        }

        button:hover {
            background: #ff4444;
            transform: scale(1.05);
        }

        .error {
            margin-top: 15px;
            color: #ff3b3b;
            font-size: 13px;
            display: none;
        }

        .shake {
            animation: shake 0.4s;
        }
    </style>

</head>

<body>

<div class="eyes-bg"></div>

<div class="container" id="page">

    <div class="box">
        <div class="kanji">呪</div>
        <h1>ACESSO RESTRITO</h1>
        <p>Digite a senha amaldiçoada para entrar...</p>

        <input id="senha" type="password" placeholder="••••••••">
        <button onclick="verificarSenha()">Entrar</button>

        <div class="error" id="error">Senha incorreta... algo está observando você.</div>
    </div>

</div>

<script>

    function verificarSenha() {
        const senha = document.getElementById("senha").value
        const error = document.getElementById("error")
        const page = document.getElementById("page")
        const sound = document.getElementById("errorSound")

        // ALTERE A SENHA AQUI
        const senhaCorreta = "bolinhadegolf"

        if (senha === senhaCorreta) {
            window.location.href = "/feiticeiro/lst"
        } else {

            error.style.display = "block"

            // SOM
            sound.currentTime = 0
            sound.play()

            // TREMOR
            page.classList.add("shake")

            setTimeout(() => {
                page.classList.remove("shake")
            }, 400)

        }
    }

</script>

<script>
   document.addEventListener("click", function() {
      const music = document.getElementById("music")
      music.volume = 0.5
      music.play()
   }, { once: true })
</script>

    <audio id="music" autoplay loop>
        <source src="/sounds/musgamagabra.mp3" type="audio/mpeg">
    </audio>

    

    <!-- SOM DO ERRO -->
    <audio id="errorSound">
        <source src="" type="audio/mpeg">
    </audio>

</body>
</html>